.Side-bar
  .user-name
    .Top-items
      .Top-items__name
        = current_user.name
      .Top-items__icons
        = link_to "#", class: "btn" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas','cog')
  .group-list
    .group

      .group-name
        techcamp
          
      .group-messege
        まだメッセージがありません

